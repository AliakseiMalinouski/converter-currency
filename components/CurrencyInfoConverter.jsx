'use client';

import classes from './CurrencyInfoConverter.module.css';

export const CurrencyInfoConverter = ({coordinates}) => {
    console.log(coordinates)
    return (
        <div className={classes.CurrencyInfoConverter}>

        </div>
    )
}